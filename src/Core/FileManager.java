package Core;

public interface FileManager {

	/**
	 * Override that method to return manager's working
	 * directory
	 * @return - Name of file manager's directory
	 */
	public String getDirectory();
}
